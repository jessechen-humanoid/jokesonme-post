## 1. 匯出前置裁切

- [x] 1.1 在 web/src/rasterize.js 的 renderToBlob 中，於 html2canvas 執行前把底圖 .bgimg 依 cover 邏輯（等比縮放、置中裁切）預先繪製成與模板畫布同比例的圖再替換 src，交付行為為「Background image aspect ratio preserved in export」——匯出的 PNG 底圖不再拉伸變形。驗證：以 npm run dev 啟動後，上傳一張 1080×1920 直式照片到公告版型（1080×1350）並下載 PNG，確認人物比例正常、畫面為上下置中裁切（各裁約 285 px），與預覽構圖一致。
- [x] 1.2 底圖比例與畫布相同時不觸發多餘裁切：上傳一張 1080×1350（或等比）照片匯出，確認 PNG 完整呈現整張圖、無裁切且無變形。驗證：目視比對匯出 PNG 與原圖構圖一致。

## 2. 三版型與既有行為驗證

- [x] 2.1 三種版型走同一條匯出路徑皆修正：同一張 4:3 橫式照片（如 4032×3024）分別套用 banner（3200×1200）、公告（1080×1350）、主視覺（1080×1350）並下載，確認三張 PNG 底圖皆為 cover 裁切、無拉伸。驗證：逐張與網頁預覽比對構圖與人物比例。
- [x] 2.2 既有匯出規格不回歸：修正後匯出的 PNG 尺寸仍為模板基準尺寸的 2 倍（1080×1350 → 2160×2700、3200×1200 → 6400×2400），且未放底圖時仍會擋下匯出並提示。驗證：檢查下載檔案的實際像素尺寸，並實測無底圖時點下載出現提示。
